#include "Interpreter.h"
#include <iostream>

 Interpreter interpreter;
int main(int argc, char *argv[]){
  interpreter.intInterpretConstructor(argc, argv);
  //interpreter.intInterpretInstruction(std::string);
}
